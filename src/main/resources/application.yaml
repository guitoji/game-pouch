spring:
  application:
    name: pouch
  jpa:
    show-sql: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:mysql://localhost:3306/gamepouchdb
    username: admin
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver
